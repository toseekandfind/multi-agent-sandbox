# Local development
REDIS_URL=redis://localhost:6379

# AWS Configuration (for deployed environment)
AWS_REGION=us-east-1
AWS_PROFILE=dan-admin

# SQS Queue URL (set after terraform apply)
SQS_QUEUE_URL=

# DynamoDB Table Name
DYNAMODB_TABLE=agent-jobs

# S3 Bucket (set after terraform apply)
S3_BUCKET=

# ECS Configuration
ECS_CLUSTER=
WORKER_TASK_DEFINITION=
WORKER_SUBNETS=
WORKER_SECURITY_GROUP=
